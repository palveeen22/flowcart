    <h1>Hello World!</h1>
    <h2> Halo <%= nama %></h2>
<h4>Daftar mahasiswa : </h4>
<% if (mahasiswa.length < 1) { %>
    <p>Data mahasiswa masih kosong..</p>
<% } else {  %>
    <% mahasiswa.forEach(m => { %>
    <ul>
        <li> Nama : <%= m.nama %>
        </li>
        <li> Email :
            <%= m.email %>
        </li>
    
    </ul>

<% }) %>
<% } %>
 